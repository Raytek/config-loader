{
  "name": "config-loader",
  "version": "1.0.0",
  "description": "An easy way to handle config files",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": ["lib/**/*"],
  "author": "Raytek",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "@types/js-yaml": "^4.0.3",
    "@types/node": "^16.9.2",
    "jest": "^27.2.0",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "ts-dot-prop": "^1.5.1"
  },
  "scripts": {
    "build": "tsc --declaration",
    "clean": "rimraf ./lib && mkdir ./lib",
    "clean:build": "yarn clean && yarn build",
    "test": "jest --config ./jest.config.js",
    "ci": "yarn test && yarn clean:build"
  }
}
